<form method="post" action="{{ path('delete_catalog', {'id': catalog.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catalog.id) }}">
    <button class="btn">Delete</button>
</form>
